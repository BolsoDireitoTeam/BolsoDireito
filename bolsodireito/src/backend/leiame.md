# sim, me leia kkkk
dentro de models vamos guardar as classes
Movimento.js vai ter a class Movimento
o arquivo vai exportar a class, pro banco, back, sla
```javascript
class Movimento() {
    constructor(param_externo){
        this.param_externo;
        this.param_interno;
    }

    metodo(variavel_param){ // dentro de uma class, não precisa da keyword function
        logica = this.param_interno - variavel_param;
        return logica
    }

    
    metodo2(variavel_param){
        logica = this.param_interno - variavel_param;
        return logica
    }
}
```

